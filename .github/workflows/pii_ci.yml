name: PII CI

on:
  push:
    branches: [ "main" ]

defaults:
  run:
    working-directory: ./pii_data_detector

jobs:
  build:
    runs-on: ubuntu-latest
    environment: pii-dev

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v2

    - name: Build
      run: |
        pip install -r requirements.txt
        python app.py